## 1. 共享查询与会话层

- [x] 1.1 从现有 MCP 和 CLI 代码中抽取共享的会话路径解析、会话加载、客户端初始化和会话验证辅助函数。
- [x] 1.2 增加本地用户配置注册表，将唯一用户配置别名映射到会话路径、标签、时间戳和可选默认用户配置。
- [x] 1.3 增加用户配置选择器解析，支持默认用户配置、显式用户配置别名和所有用户配置。
- [x] 1.4 抽取共享账户、余额和用量查询函数，返回带用户配置别名的统一内部结果形状。
- [x] 1.5 增加用户配置别名、16 位缴费户号、年份和月份的参数校验辅助函数。
- [x] 1.6 确保共享辅助函数返回安全、可操作的错误，不打印会话令牌或序列化会话内容。

## 2. MCP 工具标准化

- [x] 2.1 在可行范围内用结构化工具注册替换底层 MCP 工具请求处理。
- [x] 2.2 为用户配置列表、账户列表、余额查询、用量查询和会话验证定义输入结构和输出结构。
- [x] 2.3 增加支持默认用户配置、显式用户配置别名和所有用户配置的 MCP 输入。
- [x] 2.4 增加批量电表查询支持，返回按用户配置和电表区分的结果条目。
- [x] 2.5 成功的 MCP 结果返回 `structuredContent`，并保留兼容的人类可读 `content`。
- [x] 2.6 将校验、账户查找、用户配置、会话和上游接口失败作为带 `isError: true` 的工具结果返回。
- [x] 2.7 增加只读和非破坏性工具标注。
- [x] 2.8 将 MCP 构建产物改为零依赖 `mcp/server.cjs` 单文件，并验证无 `node_modules` 部署目录可启动。

## 3. Skill 包标准化

- [x] 3.1 更新 Skill 命令行，按确定顺序从显式参数、环境变量和 Skill 本地会话路径解析会话。
- [x] 3.2 增加 Skill 命令行用户配置命令和参数，支持列出用户配置、选择单个用户配置和查询所有用户配置。
- [x] 3.3 增加 Skill 命令行对多个户号参数和所有户号查询的支持。
- [x] 3.4 更新 Skill 命令行，成功时向标准输出输出 JSON，失败时向标准错误输出 JSON 错误。
- [x] 3.5 更新 `skills/china-southern-power-grid-stat/SKILL.md`，提供包含用户配置选择、隐私和账户列出规则的精简助手指引。
- [x] 3.6 拆分普通构建、MCP 构建、Skill 构建和本地专用 Skill 凭证打包命令。
- [x] 3.7 确保普通构建命令不会把 `session.json` 或用户配置会话文件复制到可分发目录。
- [x] 3.8 将本地专用 Skill 凭证打包位置调整为 `scripts/.csg/`，使凭证目录与 `cli.cjs` 同级。

## 4. 凭证登录流程

- [x] 4.1 保持手动登录流程写入本地会话文件，并默认不打印完整凭证。
- [x] 4.2 增加手动登录对创建或更新命名用户配置的支持。
- [x] 4.3 增加助手引导登录入口，支持选择登录方式、选择用户配置别名，并将生成的会话写入用户配置注册表。
- [x] 4.4 实现适合助手转述的二维码登录状态输出，包括链接生成、轮询、成功、超时和取消状态。
- [x] 4.5 实现短信验证码登录引导，并确保密码登录使用本地隐藏输入，而不是鼓励在对话中输入密码。
- [x] 4.6 在报告登录成功前验证新生成的会话。

## 5. 文档与生成产物

- [x] 5.1 更新 `README.md`，说明 MCP 用法、Skill 用法、构建命令、用户配置管理、会话查找和本地专用凭证打包。
- [x] 5.2 重新生成 `mcp/server.cjs` 零依赖 MCP 单文件产物。
- [x] 5.3 重新生成 `skills/china-southern-power-grid-stat/scripts/cli.cjs` 下的 Skill 打包脚本。
- [x] 5.4 如有生成或本地专用凭证/用户配置会话文件需要保护，更新忽略规则。
- [x] 5.5 更新 `README.md` 和 OpenSpec 文档，说明 `server.cjs`/`cli.cjs` 同级 `.csg/` 部署层级。

## 6. 验证

- [x] 6.1 运行 TypeScript 类型检查。
- [x] 6.2 验证没有会话文件时的 Skill 命令行错误行为。
- [x] 6.3 验证 MCP 工具列表和结构暴露。
- [x] 6.4 在本地凭证可用时验证缺失、损坏和有效会话的会话验证行为。
- [x] 6.5 验证用户配置列表、默认用户配置选择、显式用户配置选择和所有用户配置的部分失败行为。
- [x] 6.6 验证普通构建不会打包凭证，并且本地专用打包命令明确标注包含凭证。
- [x] 6.7 验证 MCP 和 Skill 在临时部署目录中均可读取可执行文件同级 `.csg/` 用户配置注册表。
